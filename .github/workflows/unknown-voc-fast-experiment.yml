name: Fast retained Unknown VOC experiment

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/unknown-voc-fast-experiment.yml
  workflow_dispatch:

concurrency:
  group: unknown-voc-autonomous-experiment
  cancel-in-progress: true

jobs:
  cancel-superseded-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by checkpointed discovery and frozen-candidate confirmation.'
