name: Disabled temporary result application

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary workflow disabled; results are being committed directly.'
